import { CreditCardBrandDirective } from './credit-card-brand.directive';

describe('CreditCardBrandDirective', () => {
  it('should create an instance', () => {
    const directive = new CreditCardBrandDirective();
    expect(directive).toBeTruthy();
  });
});
